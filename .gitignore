# Python caches
__pycache__/
*.py[cod]

# Local credentials and credential-bearing API experiments
set-elevenlabs-env.ps1
testAuth.py
getVictoria.py

# Local editor/OS files
.DS_Store
Thumbs.db

# Audacity's transient database files (the .aup3 project itself is tracked)
*.aup3-shm
*.aup3-wal

# Rebuildable assembly outputs. Keep adjacent metadata and reports tracked.
assembled/*.mp3
assembled/*_master.wav

# One-off voice/model experiments are retained locally, not versioned.
First mixed model test/
